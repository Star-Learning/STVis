name: world-gdp-shift
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas>=2.0.0
  - pyyaml>=6.0
  - requests>=2.31.0
  - tqdm>=4.65.0
  - pillow>=10.0.0
  - pip
  - pip:
      - ffmpeg-python>=0.2.0
